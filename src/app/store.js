// This will store the whole state of the app in an immutable object tree
// immutable means cannot be changed

import { configureStore } from "@reduxjs/toolkit";

export const store = configureStore({
    reducer: {
        
    }
}); 